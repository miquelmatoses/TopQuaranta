# Pin torch/torchaudio to the CPU-only build from PyTorch's own index.
# This extra index is required for the `+cpu` version specifiers below
# (the CUDA-bundled variants on PyPI are 10x larger).
--extra-index-url https://download.pytorch.org/whl/cpu

django==5.2
psycopg2-binary==2.9.10
python-decouple==3.8
dj-database-url==2.3.0
requests==2.33.1
gunicorn==23.0.0
djangorestframework==3.17.1
scikit-learn>=1.7
joblib>=1.5
# Security
argon2-cffi==25.1.0          # S10: stronger password hashing
django-axes==8.3.1           # S4: brute-force login protection
django-otp==1.7.0            # S11: TOTP 2FA for staff
qrcode[pil]==8.2             # S11: render enrollment QR (pulls Pillow)
# Audio analysis (Silero VAD)
# The `+cpu` suffix + --extra-index-url at top of this file pulls the
# CPU-only build (~200 MB vs ~2.5 GB for the default CUDA-bundled).
torch==2.5.1+cpu
torchaudio==2.5.1+cpu
silero-vad==5.1.2            # Voice Activity Detection feature for ML
